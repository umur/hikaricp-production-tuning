global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - alerts.yml

scrape_configs:
  - job_name: hikaricp-demo-app
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - hikaricp-demo-app:8080
